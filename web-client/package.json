{
  "name": "web-client",
  "version": "1.0.0",
  "description": "Browser-based user interface for arithmetic operations",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step required for static site'",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
